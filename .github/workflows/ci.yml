name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          # If you have tests, run them here. Example:
          # pytest
          echo "No tests defined yet"
      - name: Lint code
        run: |
          # If you have a linter, run it here. Example:
          # flake8 .
          echo "No linter defined yet"
      - name: Build documentation
        run: |
          # If you have documentation to build, run it here. Example:
          # sphinx-build -b html docs/ docs/_build/
          echo "No documentation build defined yet"